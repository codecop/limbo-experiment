#!/bin/bash

param = "$1"
if [ -v param ];then
    message = "$1"
else
    message = "failing"
if

git stage -A
git commit -m "[tcr] ${message}"

git revert -n HEAD
git stage -A
git commit -m "[tcr] revert"

exit 0
