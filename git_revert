#!/bin/bash
RED='\033[0;31m'
NC='\033[0m' # No Color
echo -e "${RED}"

if [ "$1" == "" ]; then
    reason="called_manual"
else
    reason="$1"
fi

git stage -A
git commit -m "[tcr] failing: ${reason}"

git revert -n HEAD
git stage -A
git commit -m "[tcr] revert"

echo -e "${NC}"

exit 0
