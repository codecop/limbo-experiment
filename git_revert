#!/bin/bash

if [ "$1" == "" ]; then
    reason="called_manual"
else
    reason="$1"
fi

git stage -A
git commit -m "[tcr] failing: ${reason}"

git revert -n HEAD
git stage -A
git commit -m "[tcr] revert"

exit 0
